@model PollModel
<section class="block block-poll">
    <div class="title">
        <strong>@T("Polls.Title")</strong>
    </div>
    <div class="listbox">
        @await Html.PartialAsync("_Poll", Model)
    </div>
</section>
